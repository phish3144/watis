# dependencies
node_modules/
.pnp
.pnp.js

# build output
out/
dist/
# Build OUTPUT — but not electron-builder's build RESOURCES, which live in the same directory by
# convention. build/installer.nsh is source and has to be tracked: ignoring it meant every Windows
# package failed in CI with "cannot find specified resource" while building fine on the machine
# that had the file. Four commits went out that way.
build/*
!build/installer.nsh
release/
*.tsbuildinfo

# electron-builder
app-update.yml
*.exe
*.dmg
*.zip
*.blockmap
latest*.yml

# native module build artifacts
prebuilds/
*.node

# test output
coverage/
test-results/
playwright-report/
blob-report/
.playwright/

# local data that must never be committed
models/
fixtures/private/
*.sqlite
*.sqlite-wal
*.sqlite-shm
loadtest-data/

# scratch files from local debugging
/shot.mjs
/dbg*.mjs

# editors / OS
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
.DS_Store
Thumbs.db
desktop.ini

# logs and env
logs/
*.log
npm-debug.log*
.env
.env.*
!.env.example

# CI / local caches
.cache/

# Written by the release gate; the numbers belong in a release note, not in git
loadtest-report.json
performance-profile.json
